# RiverNote

PokerCraftのエクスポートを対戦後に分析する、ローカル優先のポーカー学習ツールです。リアルタイム支援機能はありません。

```bash
pnpm install
pnpm dev
```

ブラウザで `http://localhost:3000` を開きます。最初からサンプルデータを表示でき、CSVを読み込むと端末内のデータに切り替わります。設計詳細は `DESIGN.md`、Supabase用DDLは `supabase/schema.sql` にあります。
